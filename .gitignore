# Rust build artifacts
/target

# Editor / OS cruft
*.swp
*~
.DS_Store

# Local secrets (none expected, but be safe)
.env
*.key
credentials*
typescript

# Local agent config (SOPs, scratch notes) — never committed
.opencode/
